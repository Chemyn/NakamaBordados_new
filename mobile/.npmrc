# expo-router arrastra vaul/@radix-ui, que declaran react-dom como peer aunque
# en React Native no se usa. Sin esto, `npm ci` falla con ERESOLVE tanto aquí
# como en los servidores de EAS Build.
legacy-peer-deps=true
